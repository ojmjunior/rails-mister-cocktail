<div class="container">
  <h1>INDEX - COCKTAILS</h1>
  
  <ul>
    <% @cocktails.each do |cocktail| %>
      <li><%= link_to("#{cocktail.name}", cocktail_path(cocktail)) %></li>
    <% end %>
  </ul>
  
  <%= link_to('Add a Cocktail', new_cocktail_path, class: "btn btn-outline-primary") %>
  
</div>
